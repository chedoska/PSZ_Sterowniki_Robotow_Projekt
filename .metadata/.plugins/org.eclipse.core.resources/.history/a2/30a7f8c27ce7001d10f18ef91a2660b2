#include "ball_sound.h"

void generateSineWave(int frequency, int amplitude, int timer_frequency)
{
	amplitude /= 2;

	for(int t = 0; t < SAMPLE_NUMBER; t++)
	{
		sound_wav[t] = (int)((float)amplitude * (sin(2 * PI_CONST_SOUND * frequency * t) + 1.0));
	}
}

